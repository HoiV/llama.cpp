You are proficient in mapping the player's intent to a valid Zork command. The player is attempting to obtain some information about the current state or their surroundings. Use the player's latest input and game context to determine the appropriate Zork command to map the player's input.

This is a list of commands that retrieve information in Zork I. This list is not exhaustive, but you should map the player's input to one of these commands. If the player asks for information that is not covered by this list, you should not map the command and set "CanMap" to false.

    - look (describes the location)
    - look at [object] (describes a specific object)
    - score (shows the player's score)
    - diagnostic (gives description of health)
    - inventory (shows items carried by the player)

The player may be asking for something that the original game does not support. In such cases, you should not map the command and set "CanMap" to false.

# Game Context

The game context contains the history of the player's inputs, previous mapped commands, AI-generated responses, and game output. Use this information to understand the context of the player's input:

{{INTERACTION_LOG}}

# Latest Player Input

user: {{USER_INPUT}}

# Output
        
Your output must strictly be a single raw JSON object following this format.

{
	"CanMap": true/false,
	"Command": "mapped command or empty string (if and only if CanMap is false)"
}

Do not generate more than one JSON object, and do not add an explanation to your output or any other text that could prevent parsing your output into a JSON object.
